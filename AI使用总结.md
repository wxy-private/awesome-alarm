# 极简实录：用 AI 搞定 UI 设计与前端代码落地的真实体验


## 一、 我的核心工作流（三步走）

目前从想法到前端页面落地，主要依赖以下三步：

1. **直接生代码（v0.dev）**
   对于常见页面，直接打开`v0.dev`。不用画图，输入想要的风格、客户端、页面严肃，它能直接吐出非常好看的界面以及配套的 React/Tailwind 代码。不好的地方就是对vue代码的支持度比较低；

2. **找灵感与定风格（Variant）**
   当 v0 生成的样式不满足需求，或者需要更定制化的设计时，我会用 `Variant`。输入想要的风格、页面必备元素，让它直接帮我生成 UI 灵感图，确立视觉基调。

3. **设计图转前端代码（Gemini 3.1 Pro）**
   拿到 Variant 生成的 UI 图后，直接把图片扔给 `Gemini 3.1 Pro`。利用它强大的多模态（视觉解析）能力，让它把 UI 样式逆向翻译成前端代码。

---

## 二、 真实工具体验与“避坑”指南

AI 工具看着都很神，但用深了全都有硬伤。：

* **Claude**
  * **体验**：在“UI 图转前端代码”以及“代码还原度”上，Claude 是绝对的、断层式的第一，细节扣得极其精准。
  * **避坑**：**太容易封号了！** 稳定性极差，常常代码写到一半账号没了，不能作为唯一依赖，必须备用其他方案。

* **Gemini 3.1 Pro**
  * **体验**：因为 Claude 容易封号，所以目前多模态“看图写代码”的重任交给了 Gemini 3.1 Pro，综合能力和上下文长度都很能打，是目前的最佳平替。

* **VS Code + Codex 插件**
  * **体验**：在 VS Code 里装 Codex 插件写代码，虽然补全不错，但**电脑发热非常严重**，资源占用优化得不好，经常风扇狂转。

* **Cursor**
  * **体验**：Cursor 确实是目前最好的 AI IDE，但**前提是你得连 Claude 模型**。
  * **避坑**：如果不使用 Claude，仅靠 Cursor 自带的或其他模型，它的“智能度”直线下降。通常只够应付局部的小范围修改，一旦让它做整体样式重构或全局修改，就总是“差点意思”，东漏一块西漏一块。

---

## 三、 总结

1. 现在的 AI 辅助开发，**不存在一个完美的“神器”**。且使用AI开发的基础是本身也要懂代码，不然遇到AI智能度不够高的时候，就会出现 **AI修复不了** 的问题，但实际上可能只是一个很小的问题。
2. 很多skills在项目规范化上能起到一定作用，但对于前端页面样式、主题进行定制化时，还是会显得捉襟见肘。

