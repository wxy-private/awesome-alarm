# AI 辅助前端开发实践：Cursor + Figma + Stitch 工具链

## 一、工具定位与选择理由

| 工具           | 为什么选择它                                                 |
| -------------- | ------------------------------------------------------------ |
| **Cursor IDE** | Cursor 不是简单的“代码补全工具”，而是一个能理解整个项目的 AI 结对编程伙伴，它让你可以用自然语言指挥代码的创建、修改、重构，同时主动帮你堵住常见的 bug 漏洞。 |
| **Figma**      | 它是设计与前端之间最顺畅的桥梁，通过 Dev Mode 和 AI 生成能力，让设计师和开发者能在同一平台上无缝协作、直接交付可用代码。 |
| **Stitch**     | 它能让你用自然语言直接生成高保真 UI 原型和可用代码，实现从“设计想法”到“前端页面”的最短路径。 |

## 二、各工具使用历程与关键交互

### 1. Cursor IDE：从“代码补全”到“结对编程”

**关键交互与对话**  

> **我（拖拽一张设计截图到 Cursor 聊天框）**：
> “这是一个移动端系统列表页，上方是筛选标签（正常、警告、异常），下面是每个系统卡片（含交易量、响应时间、响应率、正确率），系统列表支持关键字搜索。请帮我实现这个页面，使用 Vue 3 + Tailwind CSS，数据先写 mock。”

**心得**  

- **对话式重构**：不需要手动复制旧代码到新文件，Cursor 会基于选中文件生成迁移方案。  
- **显式引用**：用 `@` 符号引用文件、文档或整个目录。  
- **边界条件提醒**：Cursor 经常主动指出我可能忘记的清理工作（如定时器、事件解绑），这减少了很多隐性 bug。

### 2. Figma（含 Dev Mode 与 AI 设计生成）

**从量尺寸到直接取代码**  
Figma 推出 Dev Mode 后，只需要在 Dev Mode 下点击任意图层，右侧面板就精确显示了 CSS 属性。后来 Figma 又加入了 AI 生成设计的能力，设计师可以直接用自然语言描述界面，快速产出多个版本。

- **Dev Mode 的变量显示**：如果设计系统定义了颜色变量（如 `--primary-500`），Dev Mode 会直接显示变量名，而不是硬编码的十六进制，这对维护主题非常友好。  
- **AI 生成的边界**：Figma AI 适合生成静态布局，但对于复杂交互状态（比如加载中、错误提示、空状态），它往往遗漏，需要设计师手动补充或我开发时自己加。  
- **沟通成本降低**：以前我会因为一个像素差距来回截图提问，现在直接看 Dev Mode 的标注就能解决，设计师也很开心。

### 3. Stitch：让团队的“最佳实践”可复用

**为什么需要 Stitch**  
当团队维护着十几个业务后台，大量类似的页面（列表页、表单页、详情页）。每次新页面都是复制一个旧的，然后改字段名、改 API 地址。这不仅慢，而且会把旧页面里的 bug 也复制过去。Stitch 提供了一个“用自然语言组装代码片段”的方案。

**心得**  

- **知识沉淀**：资深开发者将常用的 hooks、组件、工具函数录制成片段，新人不用从头学起。  
- **AI 组合的智能**：Stitch 不仅会拼凑片段，还会理解片段之间的依赖关系（比如自动导入需要的库）。  
- **避免过度抽象**：工作流不能太复杂，否则参数会过多，反而不如手动写。

## 三、完整工作流串联：从设计到上线

**任务**：系统日常监控功能

| 步骤          | 工具   | 操作描述                                                     |
| ------------- | ------ | ------------------------------------------------------------ |
| 1. 设计稿     | Figma  | 设计师用 AI 生成初稿                                         |
| 2. 页面美化   | Stitch | 根据Figma初稿进行页面布局美化                                |
| 2. 前端开发   | Cursor | 打开项目，在 Cursor 中上传设计稿原型图，说“根据这个设计100%还原页面” |
| 4. 调试与完善 | Cursor | 通过指令让cursor进一步完善页面功能逻辑                       |

## 四、总体心得体会

### 1. AI 不是魔法，而是需要学习的技能
使用 Cursor 的时，经常得到不如预期的答案。后来我发现，问题越具体、引用越明确（比如“@ 文件A 和 @ 文件B，请参考文件C 的模式”），Cursor 的回答质量越高。这就像学会了一种新的“提问语言”。

### 2. 设计工具的 AI 化改变了前端的工作边界
以前拿到设计稿后，前端还要做大量的“翻译”工作。现在 Figma Dev Mode 直接给出 CSS 甚至 Tailwind 代码，前端可以更早地介入交互逻辑和状态管理。但这也要求前端更理解设计系统的变量命名，否则直接复制的代码会和项目主题脱节。

### 3. 团队级别的效率提升来自“可复用的 AI 资产”
Stitch 是我觉得最有潜力的工具。它不仅是代码片段管理器，更是将团队的隐性知识（“这个列表页应该包含哪些标准功能”“那个表单应该遵循什么校验规则”）显性化、可执行化。

### 4. 警惕 AI 生成的“看起来正确”
Cursor 生成的代码语法上通常没问题，但偶尔会有逻辑漏洞（比如忘记处理并发请求、未考虑边界值）。需要养成了一个习惯：每次 AI 生成完，要口头复述一遍它的思路，然后补充测试用例。

## 五、我的的建议

- **循序渐进**：不必一次性引入所有工具，按需引用。
- **定制你的提问模板**：在 Cursor 中好用的提问结构：  
  “上下文：@ 文件A，@ 文件B。  
  任务：我要实现 X 功能。  
  约束：不要改变现有的 Y 逻辑，兼容 Z 浏览器。  
  输出格式：先给出思路，再给出具体实现。”  
  这样得到的答案质量远高于“帮我写一个 X”。
- **永远不要完全信任 AI 生成的代码**：即使它看起来完美，也要手动跑一遍边界情况（空数据、超长文本、网络错误）。
